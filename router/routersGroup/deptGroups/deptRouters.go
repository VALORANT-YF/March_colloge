package deptGroups

import (
	"github.com/gin-gonic/gin"
)

//var deptController controller.DeptController

func DeptRouterGroups(r *gin.Engine) {
	//deptRouters := r.Group("/dept")
	//{
	//	deptRouters.PUT("/isWrite/:dept_id/:is_write_books") //更新部门是否需要写简书博客
	//}
}
